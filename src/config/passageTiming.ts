/**
 * Passage timing windows — Track 2+ (timing eval, commit window).
 * Stage water speed: see `stageProgression.ts` + StageSpeedSystem.
 * Worklet-safe.
 */

export const passageTimingTuning = {
  /** Reserved for Track 2 commit-window fractions per speed band. */
} as const;
